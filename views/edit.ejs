<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Task</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        primary: {
                            500: '#3b82f6',
                            600: '#2563eb',
                        },
                        dark: {
                            700: '#2d2d3a',
                            800: '#1e1e2d',
                            900: '#12121a',
                        }
                    },
                    animation: {
                        'fade-in': 'fadeIn 0.3s ease-out',
                    },
                    keyframes: {
                        fadeIn: {
                            '0%': { opacity: '0', transform: 'translateY(10px)' },
                            '100%': { opacity: '1', transform: 'translateY(0)' },
                        }
                    }
                }
            }
        }
    </script>
</head>

<body class="w-full min-h-screen bg-dark-900 text-gray-200 flex items-center justify-center p-4 animate-fade-in">
    <div class="w-full max-w-2xl">
        <div class="bg-dark-800 rounded-xl border border-dark-700 shadow-2xl overflow-hidden">
            <!-- Header -->
            <div class="bg-gradient-to-r from-dark-700 to-dark-800 border-b border-dark-700 p-6">
                <div class="flex items-center justify-between">
                    <h1 class="text-2xl font-bold text-white">
                        Edit Task
                    </h1>
                    <a href="/" class="text-gray-400 hover:text-white transition-colors">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24"
                            stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M6 18L18 6M6 6l12 12" />
                        </svg>
                    </a>
                </div>
            </div>

            <!-- Edit Form -->
            <form action="/edit/filename" method="post" class="p-6 space-y-6">
                <div>
                    <label for="title" class="block text-sm font-medium text-gray-300 mb-2">Previous File Name</label>

                    <input type="text"
                        class="w-full bg-dark-700 text-white px-4 py-3 rounded-lg border border-dark-600 focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-transparent"
                        name="filename" value="<%= file %>">
                </div>
                <div>
                    <label for="title" class="block text-sm font-medium text-gray-300 mb-2">New File Name</label>
                    <input type="text" name="newName" id="title"
                        class="w-full bg-dark-700 text-white px-4 py-3 rounded-lg border border-dark-600 focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-transparent"
                        placeholder="Enter new file name...">
                </div>

                <div>
                    <label for="content" class="block text-sm font-medium text-gray-300 mb-2">Task Details</label>
                    <textarea name="updatedData" id="content" rows="8"
                        class="w-full bg-dark-700 text-white px-4 py-3 rounded-lg border border-dark-600 focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-transparent resize-none"><%= data %></textarea>
                </div>

                <div class="flex justify-between pt-4 border-t border-dark-700">
                    <button type="button" onclick="window.history.back()"
                        class="px-6 py-2.5 rounded-lg border border-dark-600 text-gray-300 hover:bg-dark-700 transition-colors">
                        Cancel
                    </button>
                    <div class="flex gap-3">
                        <a href="/delete/<%= file %>">

                            <button type="button"
                                class="px-6 py-2.5 rounded-lg bg-red-600/20 border border-red-600/50 text-red-400 hover:bg-red-600/30 transition-colors">
                                Delete Task
                            </button>
                        </a>
                        <button type="submit"
                            class="px-6 py-2.5 rounded-lg bg-yellow-600 hover:bg-primary-700 text-white font-medium transition-colors flex items-center gap-2">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20"
                                fill="currentColor">
                                <path fill-rule="evenodd"
                                    d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z"
                                    clip-rule="evenodd" />
                            </svg>
                            Save Changes
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</body>

</html>